- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bashrc:
    ~/.tmux.conf:
    ~/.gitconfig:
    ~/.cheat:
    ~/.vimrc:
    ~/.vim:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
