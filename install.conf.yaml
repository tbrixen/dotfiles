- clean: ['~']

- link:
    ~/.oh-my-zsh:


- link:
    ~/.dotfiles: ''
    ~/.bashrc:
    ~/.tmux.conf:
    ~/.gitconfig:
    ~/.cheat:
    ~/.vimrc:
    ~/.vim:
    ~/.cvsignore:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]

