- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bashrc:
    ~/.tmux.conf:
    ~/.gitconfig:
    ~/.cheat:
    ~/.vimrc:
    ~/.vim:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - 
    command: vim/bundle/YouCompleteMe/install.py
    description: Installing you completeme
    stdout: true
    stderr: true


